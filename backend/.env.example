# Configurações do banco de dados PostgreSQL
PGHOST=localhost
PGPORT=5432
PGDATABASE=vlib
PGUSER=vlib_user
PGPASSWORD=sua_senha_aqui

# Porta do servidor backend
PORT=4000

# JWT Secret para autenticação
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Opcional: URL completa de conexão (alternativa aos campos individuais acima)
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/vlib
