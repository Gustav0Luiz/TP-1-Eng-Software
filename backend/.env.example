# ================================
# Configurações da aplicação Node
# ================================

# Porta em que o servidor Express vai rodar
# Exemplo: PORT=4000
PORT=

# ================================
# Configurações do PostgreSQL
# ================================

# Endereço do servidor do banco
# Normalmente "localhost" se for rodar na sua máquina
# Ou o IP/nome do host se estiver em outro servidor
PGHOST=

# Porta de conexão do PostgreSQL
# Padrão é 5432
PGPORT=

# Nome do banco de dados que a aplicação vai usar
# Exemplo: PGDATABASE=vlib
PGDATABASE=

# Usuário com permissão de acesso ao banco
# Exemplo: PGUSER=vlib_user
PGUSER=

# Senha do usuário do banco
# Exemplo: PGPASSWORD=senha
PGPASSWORD=


# ================================
# Autenticação (JWT)
# ================================

# Chave secreta usada para assinar tokens JWT (use uma string bem aleatória)
JWT_SECRET=
# Tempo de expiração do token (ex.: "7d", "1h")
JWT_EXPIRES_IN=

# ================================
# Configurações de envio de e-mail (SMTP)
# ================================

# Host do servidor SMTP (provedor de e-mail)
# Exemplos:
#   Gmail → smtp.gmail.com
#   Outlook → smtp.office365.com
#   Provedor próprio → mail.seudominio.com
SMTP_HOST=

# Porta do servidor SMTP
# Padrões mais comuns:
#   587 → STARTTLS (mais usado)
#   465 → SSL/TLS
#   25  → legado (pouco usado, muitas vezes bloqueado por provedores)
SMTP_PORT=

# Define se a conexão SMTP será segura
# true  → SSL/TLS
# false → STARTTLS (a mais comum)
SMTP_SECURE=

# Usuário da conta SMTP (geralmente o e-mail completo)
# Exemplo: name@gmail.com
SMTP_USER=

# Senha da conta SMTP
# Para Gmail e outros provedores, pode ser necessário gerar uma "senha de aplicativo"
SMTP_PASS=

# Remetente padrão dos e-mails enviados pelo sistema
# Formato recomendado: "Nome <email@dominio.com>"
# Exemplo: "Vlib <no-reply@seusistema.com>"
MAIL_FROM=
